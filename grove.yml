name: grove-meta
description: Grove package manager and command delegator
binary:
  name: grove
  path: ./bin/grove

logging:
  file:
    enabled: true
    format: json

context:
  default_rules_path: .cx/dev-no-tests.rules
